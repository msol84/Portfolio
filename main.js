let screenWidth = window.innerWidth;
const selection = document.getElementsByClassName("firstLook")[0];
console.log(selection);

window.addEventListener('resize', () => {
    screenWidth = window.innerWidth;
    if (screenWidth >= 1440) {

        //selection. = "reverse";

    }
})

